<%= form_for(colony) do |f| %>
  <% if colony.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(colony.errors.count, "error") %> prohibited this colony from being saved:</h2>

      <ul>
      <% colony.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :contact_id %>
    <%= f.text_field :contact_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
